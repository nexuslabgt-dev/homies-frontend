---
import Base from '@/layouts/Base.astro';
import { supabase } from '@/lib/supabase';
import ReviewCard from '@/components/ReviewCard.astro';

const { data: { session } } = await supabase.auth.getSession();
if (!session) return Astro.redirect('/login');

const userId = session.user.id;

const { data: reviews } = await supabase
  .from('reviews')
  .select(`
    *,
    bookings:booking_id(service:service_id(name), caregiver:caregiver_id(profiles:profile_id(full_name)))
  `)
  .eq('author_id', userId)
  .order('created_at', { ascending: false });

const { data: completedBookings } = await supabase
  .from('bookings')
  .select(`
    *,
    services:service_id(name),
    caregivers:caregiver_id(profiles:profile_id(full_name))
  `)
  .eq('client_id', userId)
  .eq('status', 'completed')
  .order('created_at', { ascending: false });

const reviewedBookingIds = reviews?.map(r => r.booking_id) || [];
const pendingReviews = completedBookings?.filter(b => !reviewedBookingIds.includes(b.id)) || [];
---

<Base title="Mis Reseñas" headerVariant="dark">
  <section class="min-h-screen bg-gray-50 dark:bg-gray-950 py-12">
    <div class="container mx-auto px-4 lg:px-8">
      <div class="mb-8">
        <h1 class="font-serif text-3xl font-bold text-gray-900 dark:text-white">
          Mis Reseñas
        </h1>
        <p class="mt-1 text-gray-600 dark:text-gray-400">
          Gestiona las reseñas que has dejado.
        </p>
      </div>

      {pendingReviews.length > 0 && (
        <div class="mb-8 bg-primary-900/20 border border-primary-700 rounded-xl p-6">
          <h2 class="text-lg font-semibold text-white mb-4">Reseñas pendientes</h2>
          <p class="text-gray-400 text-sm mb-4">
            Tienes {pendingReviews.length} reserva{pendingReviews.length > 1 ? 's' : ''} completada{pendingReviews.length > 1 ? 's' : ''} sin reseñar.
          </p>
          <div class="space-y-3">
            {pendingReviews.slice(0, 3).map((booking) => (
              <div class="flex items-center justify-between bg-gray-800/50 rounded-lg p-4">
                <div>
                  <p class="font-medium text-white">{booking.services?.name || 'Servicio'}</p>
                  <p class="text-sm text-gray-400">con {booking.caregivers?.profiles?.full_name || 'Cuidador'}</p>
                </div>
                <a
                  href={`/dashboard/bookings?review=${booking.id}`}
                  class="px-4 py-2 bg-primary-600 hover:bg-primary-700 text-white text-sm font-medium rounded-lg transition-colors"
                >
                  Dejar reseña
                </a>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="mb-6">
        <h2 class="text-xl font-semibold text-white">Reseñas escritas</h2>
      </div>

      {reviews && reviews.length > 0 ? (
        <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
          {reviews.map((review) => (
            <div class="relative">
              <ReviewCard review={review} />
              <div class="absolute top-4 right-4 flex gap-2">
                <button
                  class="p-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-gray-400 hover:text-white transition-colors"
                  title="Editar"
                >
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                  </svg>
                </button>
                <button
                  class="p-2 bg-gray-700 hover:bg-red-600 rounded-lg text-gray-400 hover:text-white transition-colors"
                  title="Eliminar"
                >
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                  </svg>
                </button>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div class="text-center py-16 bg-gray-900/50 rounded-xl">
          <svg class="mx-auto h-16 w-16 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
          </svg>
          <h3 class="mt-4 text-lg font-medium text-gray-400">No has escrito ninguna reseña</h3>
          <p class="mt-2 text-gray-500 text-sm">Completa una reserva para dejar tu primera reseña.</p>
          <a
            href="/services"
            class="inline-block mt-6 px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-medium rounded-xl transition-colors"
          >
            Ver servicios
          </a>
        </div>
      )}
    </div>
  </section>
</Base>
